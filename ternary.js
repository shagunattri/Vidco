function equal(a,b) {
    return a === b ? true : false;

    return a === b;
}
// equal(1,1);
var out =  equal(1,1);
console.log(out);

function checkSign(num) {
    return num > 0 ? "+" : num < 0 ? "-" : "0";
}

console.log(checkSign(0));
